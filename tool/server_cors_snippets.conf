# Reference only — paste/adapt into nginx, Traefik, or your API gateway in front of Couchbase / Sync Gateway / custom server.
#
# Replace $cors_origin with your Flutter web origin, e.g. http://localhost:12345
# (use the exact origin shown in the browser address bar when running flutter run -d chrome).

# --- nginx (location / or specific API prefix) ---
# add_header 'Access-Control-Allow-Origin' '$cors_origin' always;
# add_header 'Access-Control-Allow-Credentials' 'true' always;
# add_header 'Access-Control-Allow-Methods' 'GET, POST, PUT, PATCH, DELETE, OPTIONS' always;
# add_header 'Access-Control-Allow-Headers' 'Authorization, Content-Type, X-Tenant-Id, X-Tracking-Id, Accept' always;
# if ($request_method = 'OPTIONS') {
#   return 204;
# }
